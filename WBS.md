# 台股資訊儀表板 — 工作分解結構 (WBS)

## 專案目標
打造一個可透過網頁遠端瀏覽（手機、平板、其他電腦皆可）的台股資訊儀表板，擴充目前只能在本機執行的靜態頁面與桌面小工具。核心功能：

1. 台股殖利率排行（1–20 名，當年度殖利率）
2. 5 年平均殖利率排行（1–20 名）
3. 10 年平均殖利率排行（1–20 名）
4. 除權息日程表
5. 合理價試算
6. 風險試算

## 現況盤點（已存在的基礎，不用從零開始）
- `get_data.py`：用 TWSE OpenAPI（`BWIBHT_ALL`）抓「當年度」全市場殖利率，存成 `data.json`，目前只做 Top 50、只有殖利率欄位。
- `index.html`：靜態頁面用 `fetch` 讀 `data.json` 畫表格。**目前檔案裡有殘留的重複 HTML 片段（第 43–93 行是誤貼進去的完整頁面），需要清理重寫。**
- `line_push.py` + `run_daily_push.ps1`：讀 `data.json` 前 5 名，透過 LINE Bot 廣播訊息，目前靠本機工作排程器觸發。
- `c:\stock\stock_widget.py`（另一個現有專案，桌面 tkinter 小工具）裡已有可直接重用的邏輯：
  - `fetch_year_snapshot` / `fetch_yield_history`：用 TWSE `BWIBBU_d`（`selectType=ALL`）逐年抽樣「全市場」殖利率快照 — 這正是 5 年/10 年排行需要的抓取方式。
  - `compute_valuation`：用歷史殖利率快照算「合理價／便宜價／昂貴價」。
  - `fetch_dividend_valuation_from_yahoo`：ETF（`BWIBBU_d` 算不出來的品種，如 00878）改用 Yahoo Finance 配息事件計算，回傳格式跟 `compute_valuation` 一致可共用顯示邏輯。

  這兩支程式分別解決了「單檔查詢」與「全市場排行」的一半，這個專案要把抓取邏輯整合、擴大到全市場排行規模。

## 架構決策與假設（請確認或提出修改）
> 以下是拆分任務時採用的預設決策，都是可逆的。如果想法不同（例如要用常駐後端而不是靜態頁面），說一聲就會照新方向重新拆解。

1. **維持「靜態網頁 + 排程產生 JSON」架構**，不另外架設常駐後端伺服器。殖利率排行、除權息日程都不是即時性資料，一天更新一次即可；靜態網站部署最簡單、最省成本，也最容易做到「跨裝置遠端瀏覽」。
2. **部署平台：GitHub Pages**，資料更新交給 **GitHub Actions 排程（cron）** 執行 Python 抓取腳本、重新產生 JSON、自動 commit push（Pages 自動重新部署）。好處是不必依賴自己的電腦一直開機（目前 `run_daily_push.ps1` 要靠本機排程器）。這代表：
   - 這個資料夾需要 `git init` 並推到 GitHub（目前不是 git repo）。
   - 這屬於「建立外部帳號/服務串接」的動作，實際執行前會再另外確認一次，不會自動進行。
3. **上市 + 上櫃都要涵蓋**：目前 `get_data.py` 只查 TWSE（上市），`stock_widget.py` 也註明「上櫃股票目前無法計算殖利率歷史」。排行榜若只看上市會漏掉不少高股息上櫃股，因此工作項目包含「加上 TPEx（櫃買中心）資料源」的任務。
4. **風險試算的定義（已確認，取代原先的草案）**：採「定存股風險試算」— 使用者輸入「買入股價」「期末股價」「今年配息」「持有年數」四個數字，算出：
   - 投資報酬率 = (期末股價 − 買入股價 + 今年配息 × 持有年數) ÷ 買入股價 × 100%
   - 最低可容忍股價（報酬率打平點）= 買入股價 − 今年配息 × 持有年數
   讓使用者知道「股價要跌到多少，靠配息還能保本」，殖利率越高、持有年數越長，可容忍的下跌空間越大。這是**純公式的互動試算工具**，不需要抓歷史多年資料，只需要「目前股價」與「今年配息」當輸入框預設值（來自任務 02 的資料），細節見 [`tasks/06-risk-calculation.md`](tasks/06-risk-calculation.md)。
5. **合理價試算的定義（已確認）**：採台股存股圈常見的「股海老牛式」固定倍數估價法，跟 `stock_widget.py` 原本依個股自己歷史平均殖利率動態換算的 `compute_valuation()` 不同：
   - 基準：該股票**最近 5 年**平均現金股利
   - 合理價 = 平均現金股利 × 20（年息 5%）
   - 便宜價 = 平均現金股利 × 16（年息 6.25%）
   - 昂貴價 = 平均現金股利 × 32（年息 3.125%）
   這三個倍數對所有股票固定不變，不是逐檔動態計算。資料抓取管線（近 5 年股利換算）仍可沿用 `stock_widget.py` 的 `fetch_year_snapshot` / `fetch_yield_history` 抓法，只是最後套用的公式改成固定倍數。全市場預先算好存進 JSON；前端做「輸入代碼查詢」介面，不做即時運算。細節見 [`tasks/05-fair-value-calculation.md`](tasks/05-fair-value-calculation.md)。
6. **除權除息日程表的資料來源還沒確認**，這是任務 04 第一步要先研究：候選來源是 TWSE 開放資料（openapi.twse.com.tw）與 TPEx 開放資料（openapi.tpex.org.tw）裡的除權除息預告資料集，需要先找到正確的資料集路徑並驗證欄位（不確定的 API 路徑不能用猜的，要先實際查證）。

## 里程碑
| 里程碑 | 內容 | 對應任務 |
|---|---|---|
| M1 資料層完成 | 三種殖利率排行、除權息日程、合理價的抓取/計算腳本都能在本機跑出正確 JSON；風險試算公式邏輯驗證完成 | 01–06 |
| M2 自動化完成 | 排程每天自動更新資料並發布，不需人工介入 | 07 |
| M3 前端完成 | 六大功能頁面本機開瀏覽器都能看到正確資料，手機版排版正常 | 08–10 |
| M4 上線 | 透過網址在其他裝置（手機、平板、其他電腦）都能連上看到內容 | 11 |
| M5 收尾 | 測試、文件、LINE 推播同步更新新排行資料 | 12 |

## 工作分解結構總覽
0. 專案初始化與環境設定
1. 資料擷取－當年殖利率排行
2. 資料擷取－5 年／10 年平均殖利率排行
3. 資料擷取－除權除息日程表
4. 合理價試算邏輯
5. 風險試算邏輯
6. 自動化排程管線
7. 前端－殖利率排行榜頁面
8. 前端－除權息日程表頁面
9. 前端－合理價與風險試算工具頁面
10. 部署與遠端存取驗證
11. QA、文件與 LINE 推播整合更新

（任務檔案編號從 `01` 開始，對應細節見 `tasks/` 資料夾，`tasks/README.md` 是派送順序索引。）

## 相依關係
```
01 專案初始化
 ├─▶ 02 當年殖利率 ──────┬──────────────────────────▶ 06 風險試算（公式）──┐
 ├─▶ 03 5年/10年殖利率 ───┴─▶ 05 合理價試算 ──────────────────────────────┤
 ├─▶ 04 除權息日程表 ─────────────────────────────────────────────────────┼─▶ 07 自動化排程 ─▶ 11 部署 ─▶ 12 QA/文件/LINE
 └────────────────────────────────────────────────────────────────────────┘

02 + 03 + 04 + 05 + 06 全部完成後才開始：
 08 排行榜頁面 / 09 除權息日程頁面 / 10 試算工具頁面（三者可平行；10 需要 05 + 06）
 08 + 09 + 10 完成後才進 11 部署
```

## 執行進度（2026-07-29）

任務 01–10 已經實際動手實作並在本機測試通過，細節見根目錄 [`README.md`](README.md)：
- 01 專案初始化：`data/` `scripts/` `assets/` 資料夾已建立。**git 已安裝**（winget 安裝
  2.55.0.3），已執行 `git init` 並建立第一個 commit（`7c0eb25`），本機 repo 已就緒。
- 02–05 資料擷取/計算腳本（`scripts/*.py`）都已實作並實際執行成功，`data/` 底下已經有
  跑出來的真實資料。過程中發現並修正兩個問題：舊版 `get_data.py` 用的 `BWIBHT_ALL`
  端點已失效（已修正為 `BWIBBU_ALL`）；歷史殖利率資料偶爾出現離譜異常值（已加合理性過濾）。
- 04 除權息日程表的資料來源已經查證確認（見下方），不再是開放問題。
- 06 風險試算公式已實作成 `assets/js/risk.js`，並用需求方給的範例數字驗證過。
- 07 GitHub Actions 排程檔已寫成草稿（`.github/workflows/update-data.yml`）並驗證 YAML
  語法正確、每個步驟對應的腳本路徑都存在，但**還沒有實際推上 GitHub 執行過**，因為
  repo 還沒推到遠端（本機 repo 已就緒,只差 remote）。
- 08–10 三個前端頁面已完成並用 headless 瀏覽器實際測試過（分頁切換、搜尋篩選、
  合理價查詢、風險試算表單都驗證數字正確）。

- 11 部署已完成：repo 推到 `github.com/tsejenwang/tw-stock-dividend-dashboard`（public），
  GitHub Pages 已開啟，正式網址 `https://tsejenwang.github.io/tw-stock-dividend-dashboard/`，
  用 headless 瀏覽器實測三個頁面的資料都正常載入。

12（QA/文件/LINE 整合）還沒執行，原因見下方開放問題。

## 尚待確認的開放問題
- [ ] 任務 07 收尾：`.github/workflows/update-data.yml` 還沒有實際驗證過在 GitHub 上
  自動觸發、`git push` 回去是否成功（需要確認 repo 的 Settings → Actions → General 裡
  「Workflow permissions」有開「Read and write permissions」，否則排程最後一步會失敗）。
  目前線上的 `data/` 資料是手動跑腳本後推上去的一次性快照，不會自動更新。
- [ ] 任務 12：LINE 推播（`line_push.py`）還沒整合新的排行/日程/試算資料，目前只修好了
  舊版 `get_data.py` 的失效端點，維持現況可運作

風險試算定義、除權息日程表資料來源都已確認（見上方架構決策與執行進度），不再是開放問題。

## 除權息日程表資料來源（任務 04 研究結果）
- 上市：`openapi.twse.com.tw/v1/exchangeReport/TWT48U_ALL`（上市股票除權除息預告表）
- 上櫃：`www.tpex.org.tw/openapi/v1/tpex_exright_prepost`（上櫃股票除權除息預告表）

這兩個路徑是從 TWSE / TPEx 官方 swagger 規格檔（`openapi.twse.com.tw/v1/swagger.json`、
`www.tpex.org.tw/openapi/swagger.json`）裡查到並實測確認可用，不是用猜的。
